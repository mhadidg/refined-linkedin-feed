{
  "name": "linkedin-feed-filter",
  "type": "module",
  "scripts": {
    "build": "web-ext lint -s src && web-ext build -s src -a artifacts",
    "test": "jest -i --colors --verbose --setupFiles dotenv/config",
    "prettier": "prettier --check **/*.{js,ts,json}",
    "prettier:fix": "prettier --write **/*.{js,ts,json}"
  },
  "jest": {
    "rootDir": "test",
    "preset": "jest-puppeteer",
    "testTimeout": 60000
  },
  "devDependencies": {
    "dotenv": "^10.0.0",
    "expect-puppeteer": "^5.0.4",
    "jest": "^27.0.6",
    "jest-puppeteer": "^5.0.4",
    "prettier": "^2.6.2",
    "prettier-plugin-jsdoc": "^0.3.38",
    "puppeteer": "^10.1.0",
    "web-ext": "^6.2.0"
  }
}
