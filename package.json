{
  "name": "linkedin-feed-filter",
  "type": "module",
  "scripts": {
    "test": "jest",
    "build": "web-ext lint -s src && web-ext build -s src -a artifacts"
  },
  "jest": {
    "rootDir": "test",
    "preset": "jest-puppeteer",
    "testTimeout": 60000
  },
  "devDependencies": {
    "dotenv": "^10.0.0",
    "expect-puppeteer": "^5.0.4",
    "jest": "^27.0.6",
    "jest-puppeteer": "^5.0.4",
    "puppeteer": "^10.1.0",
    "web-ext": "^6.2.0"
  }
}
